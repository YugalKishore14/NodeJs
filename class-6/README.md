# NODE + NPM Commands

> npm -v
> npm -help
> npm -init or npm init -y

# add js code with package

> script{

    "start" = [npm start]
    "yugal" = [npm run dev]

}

# package manager

> npm install lite-server --save-dev
